---
description: DB 조회 연결을 설정한다 (DB 이름·아이디·비밀번호 1회 입력)
---

`konclo-db-setup` 스킬의 절차를 그대로 따라 DB 조회 연결을 설정한다.

절차 정본은 `skills/konclo-db-setup/SKILL.md` 한 곳이다 — 이 파일에 절차를 복사해 두지 않는다.
(Codex 는 이 플러그인의 `commands/` 를 읽지 않고 스킬만 읽으므로, 절차가 두 곳에 있으면
한쪽만 고쳐져 두 환경의 동작이 갈린다.)
